const addTrain = (train) => {

    const {
        name,
        destination,
        firstTime,
        frequency,
        // minutesAway
    }
}
//name, destination, first time (military), frequency (min), minutes away.

const calculateNextArrivalTime = (train) => {

}

// should be relative to current time


// Bonus
// update times every minute.